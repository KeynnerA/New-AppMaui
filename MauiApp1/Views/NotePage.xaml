<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MauiApp1.Models"
             x:Class="MauiApp1.Views.NotePage"
             Title="NotePage">
    <ContentPage.BindingContext>
        <local:NotasViewModel />
    </ContentPage.BindingContext>

    <Grid RowDefinitions="Auto,*" ColumnDefinitions="*">

        <Editor Grid.Row="0"
                x:Name="TextEditor"
                Placeholder="Enter your note here"
                HeightRequest="100"/>

        <VerticalStackLayout Grid.Row="1" Padding="10" Spacing="10">

            <Grid ColumnDefinitions="*,*" ColumnSpacing="4">
                <Entry x:Name="NotaEntry" Placeholder="Escribe tu nota aquí" Text="{Binding NotaEntryText}" />
                <Button Grid.Column="0"
                        Text="Save"
                        Clicked="SaveButton_Clicked"
                        Command="{Binding GuardarNotaCommand}"
                        CommandParameter="{Binding Text, Source={x:Reference NotaEntry}}" />
                <Button Grid.Column="1"
                        Text="Delete"
                        Clicked="DeleteButton_Clicked"/>
            </Grid>

            <CollectionView ItemsSource="{Binding Notas}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnDefinitions="*,Auto" Padding="10">
                            <VerticalStackLayout Grid.Column="0">
                                <Label Text="{Binding Titulo}" FontAttributes="Bold"/>
                                <Label Text="{Binding Contenido}" />
                                <Label Text="{Binding Fecha}" FontSize="10" TextColor="Gray"/>
                            </VerticalStackLayout>
                            <ImageButton Grid.Column="1"
                             Source="icon_edit.png"
                             WidthRequest="10"
                             HeightRequest="10"
                             BackgroundColor="Transparent"
                            Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditarNotaCommand}"
                            CommandParameter="{Binding .}" />
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </Grid>
</ContentPage>
